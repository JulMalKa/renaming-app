# renaming-app

A Python-based tool designed to automate the processing, categorization, and renaming of PDF documents (specifically vehicle-related documents like Registration Certificates, COC, and ECMT).

The script scans the user's Downloads folder, uses OCR to extract vehicle information (VIN and License Plate Number), detects QR codes for proper image orientation, and renames files according to a standardized naming convention.